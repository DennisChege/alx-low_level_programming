#include <stdio.h>

/**
 * main - print the phrase with the proper grammar,
 * but the outcome is a piece of art
 * Return: 0 if existed properly, non-zero otherwise
 */
int main(void)
{
	printf("with proper grammar, but the outcome is apiece of art,/n");
	return(0);
}
