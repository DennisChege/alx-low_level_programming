#include <stdio.h>

/**
 * main - prints single digit numbers of base 10
 * startine from 0, followed by a new line
 * Return: Always 0 if successful
 */
int main(void)
{
	int n;

	for (n = 0; n < 0; n++)
		printf("%d", n);
	printf("\n");
	return (0);

}
